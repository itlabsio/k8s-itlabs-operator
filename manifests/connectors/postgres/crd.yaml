---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: postgresconnectors.itlabs.io
spec:
  scope: Cluster
  group: itlabs.io
  names:
    kind: PostgresConnector
    plural: postgresconnectors
    singular: postgresconnector
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          description: Postgres connector list defines instances of Postgres.
          type: object
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values.'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
                object represents. Servers may infer this from the endpoint the client
                submits requests to. Cannot be updated. In CamelCase.'
              type: string
            metadata:
              type: object
            spec:
              type: array
              items:
                description: Postgres connector defines instance of Postgres.
                type: object
                properties:
                  name:
                    description: name of Postgres connector, should be unique for cluster.
                    type: string
                  vaultpath:
                    description: path to the secret in vault with credentials to pg instance for operator
                    type: string
